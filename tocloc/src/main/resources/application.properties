# Nome da aplicação
spring.application.name=tocloc

# Configuração do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/tocloc?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# Configuração do Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update



# Configuração de log (opcional, para depuração de SQL)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# Configuração de perfis (exemplo: 'dev' e 'prod')
# spring.profiles.active=dev


